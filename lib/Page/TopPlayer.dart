import 'package:flutter/material.dart';

class Topplayer extends StatelessWidget {
  const Topplayer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ini Top player'),
      ),
    );
  }
}